# 目標環境：Windows 10 / 11 + Python 3.12
# （已明確放棄 Windows 7 —— Qt6 不支援，且 Python 3.8 是最後一版支援 Win7 的。
#   見 docs/ai/03-decisions.md 的 D2）
#
# 以下是 v1.0.0 實際驗證過的版本組合。
# 沒有網路的話請看 docs/OFFLINE-INSTALL.md。

pywin32==306
PySide6==6.7.3

# 選配：WPD 診斷探針用（core/wpd_probe.py，決策 D21）。
# ★ 沒裝也完全不影響備份功能 —— 診斷報告只會少「WPD 探針」那一段。
#   它在 runtime 產生 COM wrapper，與 PyInstaller 有已知衝突，
#   所以程式裡每一步都獨立 try/except，探針掛掉不會影響任何其他功能。
# （2026-09-13 查 PyPI 的最新版；本專案尚未在 Windows 上實測過任一版本，
#   選最新是因為它比 1.4.6 多了一年半的 PyInstaller / Python 3.12 修正。）
comtypes==1.4.16
